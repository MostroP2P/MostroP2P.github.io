---
import { type Locale, t } from '../i18n/translations';

interface Props { 
  locale: Locale; 
}
const { locale } = Astro.props;
const i = t(locale);

const features = [
  {
    title: i.features.feature1,
    desc: i.features.feature1Desc,
    icon: "🔒"
  },
  {
    title: i.features.feature2,
    desc: i.features.feature2Desc,
    icon: "🛡️"
  },
  {
    title: i.features.feature3,
    desc: i.features.feature3Desc,
    icon: "🔑"
  },
  {
    title: i.features.feature4,
    desc: i.features.feature4Desc,
    icon: "🌐"
  },
  {
    title: i.features.feature5,
    desc: i.features.feature5Desc,
    icon: "🔗"
  },
  {
    title: i.features.feature6,
    desc: i.features.feature6Desc,
    icon: "🆓"
  }
];
---

<section id="features" class="section">
  <div class="container">
    <h2 class="section-title">{i.features.title}</h2>
    <p class="section-subtitle">{i.features.subtitle}</p>
    
    <div class="features-grid">
      {features.map(feature => (
        <div class="feature-card glass card">
          <div class="feature-icon">{feature.icon}</div>
          <h3>{feature.title}</h3>
          <p>{feature.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 24px;
    margin-top: 48px;
  }

  .feature-card {
    text-align: center;
    position: relative;
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-5px);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 20px;
    display: block;
  }

  .feature-card h3 {
    color: var(--text-primary);
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .feature-card p {
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 0.95rem;
  }

  /* Mobile styles */
  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .feature-card {
      padding: 24px;
    }

    .feature-icon {
      font-size: 2.5rem;
    }
  }
</style>